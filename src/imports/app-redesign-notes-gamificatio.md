Double check the "tasks" section--I can't create a new task. Also, let the user adjust the time of the quick start timer, and add custom tasks from there--they should not have to pull a task from the task section. 

Restyle the entire app with a new design system. Use the following specifications consistently across every element: Background colors — primary #f7f5f2, white panels #ffffff, subtle hover/secondary #f0ede8. Sidebar — deep navy #1a1f2e with secondary #242938. Text colors — primary ink #1a1f2e, mid #5a5f72, light/muted #9499a8. Accent colors — blue #4f6ef7 with light tint #eaedfe, green #2dba7e with tint #e4f7f0, amber #f0a030 with tint #fef3e2, rose #e85d75 with tint #fde8ec. Borders — #e8e4de. Fonts — import DM Sans (weights 300, 400, 500) and DM Serif Display (regular and italic) from Google Fonts. Use DM Sans for all UI text, labels, buttons, and body. Use DM Serif Display for all page titles, section headings, and the app logo. Navigation sidebar uses deep navy background with white text at 55% opacity for inactive items and full white for active, with a blue tinted highlight on the active item. Events and calendar blocks are color coded by category — blue for focus and work blocks, rose for meetings, amber for planning and admin, green for personal and lunch — each using the light tint as background with a 3px left border in the full accent color. Tasks use a checkbox with rounded corners, blue fill when complete. Mini calendar highlights today in solid blue with white text. The current focus block at the bottom of the sidebar panel uses a dark navy gradient background with white text and a blue progress bar. All cards and panels use 8px border radius, subtle box shadow (0 2px 16px rgba(26,31,46,0.07)), and 1px borders in the border color. The overall feel should be clean, elevated, and professional — like a modern productivity tool with quiet confidence. 

Add a light, professional gamification layer to the app with the following features. First, a daily completion score — at the end of each day calculate a score based on tasks completed versus tasks planned, displayed as a percentage with a small circular progress ring in the accent blue color in the top bar. Second, task completion satisfaction — when a user checks off a task, trigger a subtle checkmark animation and a very brief green flash on the task row, nothing loud or childish, just a moment of visual reward. Third, a weekly productivity digest — every Monday morning show a clean one-screen summary of last week: tasks completed, meetings attended, focus hours logged, and best day of the week highlighted. Style it like an elegant report card, not a game screen. Fourth, focus block streaks — if the user completes their scheduled focus blocks for three or more consecutive days, show a small unobtrusive streak indicator next to the focus block on the schedule. Fifth, a subtle weekly score trend — in the sidebar below the mini calendar, add a tiny sparkline chart showing daily completion scores across the past seven days so the user can see their momentum at a glance. Keep all gamification understated and professional — satisfying micro-interactions rather than badges or loud rewards. Store all data in localStorage so it persists between sessions.

Edit the Notes section to the app and model it after Windows Sticky Notes but elevated to match our existing design aesthetic. It should work as follows: in the sidebar navigation add a Notes item with a small note icon. When clicked it opens a Notes view in the main content area displaying a grid of note cards. Each note card has a soft colored background — use muted tints of our existing accent colors (soft blue, soft amber, soft rose, soft green) that the user can choose from when creating a note. Each card shows the first few lines of the note text, the date it was last edited, and a small delete button on hover. Clicking a card opens it in an inline editor in the center of the screen with a clean minimal textarea — no toolbars, just the text. The editor auto-saves as the user types with a subtle 'Saved' confirmation that fades in and out. A large '+' button in the top right of the Notes view creates a new blank note and prompts the user to pick a color. Notes can have an optional title or just free text. Keep the cards in a masonry-style grid so they feel organic and informal, like real sticky notes on a desk, but rendered with our clean modern aesthetic — rounded corners, soft shadows, DM Sans typography. Store all notes in localStorage so they persist between sessions. The overall feel should be fast, frictionless, and satisfying — like reaching for a sticky note but finding something much nicer. Link the colors of the notes with the colors of the schedule categories, adding in a white note category whose color is not connected with the schedule colors.
